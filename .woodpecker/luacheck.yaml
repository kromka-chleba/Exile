when:
  - event: [pull_request, push]
  - branch: action_test
steps:
  - name: luacheck
    image: registry.gitlab.com/pipeline-components/luacheck:latest
    commands:
      - luacheck ${CI_PIPELINE_FILES}

